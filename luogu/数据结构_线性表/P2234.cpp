#include<iostream>

using namespace std;
const int N = 32767 +10;
int n, a[N], d[N], q[N], idx;

int main()
{
    cin >> n;
    for(int i = 0; i < n; i++)  cin >> a[i];
    d[0] = a[0];
    q[0] = 0;
    for(int i = 1; i < n; i++)
    {

    }


    return 0;
}