#include<iostream>

using namespace std;

const int N = 2e4 + 10, M = 2e5 + 10;
struct {
    int from, to, w;
} p[M];
int n, m, L, R, cnt, head[M];

int main(){
    cin >> n >> m;
    for(int i = 0; i < n; i++)
    {
        
    }

    return 0;
}